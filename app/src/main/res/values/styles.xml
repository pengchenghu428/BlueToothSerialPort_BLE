<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Remove Window Title. -->
        <item name="windowNoTitle">true</item>
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Menu Button Style -->
    <style name="menu_button">
        <item name="android:layout_width">48dp</item>
        <item name="android:layout_height">48dp</item>
        <item name="android:background">?android:attr/actionBarItemBackground</item>
    </style>

    <!-- ToolBar Base Theme -->
    <style name="Theme.ToolBar.Base" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:textColorPrimary">@color/colorAccent</item>
        <item name="android:textSize">16dp</item>
        <item name="actionOverflowMenuStyle">@style/OverflowMenuStyle</item>
    </style>

    <!-- OverFlow Menu Style -->
    <style name="OverflowMenuStyle" parent="@style/Widget.AppCompat.Light.PopupMenu.Overflow">
        <item name="overlapAnchor">false</item>
    </style>

    <!--activity_set_labels style-->
    <!--linearlayout-->
    <style name="SetLabels_LinearLayout">
        <item name="layout_width">match_parent</item>
        <item name="layout_height">wrap_content</item>
    </style>
    <style name="SetLabels_LinearLayout_TextView">
        <item name="layout_width">wrap_content</item>
        <item name="layout_height">wrap_content</item>
    </style>
    <style name="SetLabels_LinearLayout_Edit">
        <item name="layout_width">0dp</item>
        <item name="layout_height">wrap_content</item>
        <item name="layout_weight">1</item>
    </style>

</resources>
